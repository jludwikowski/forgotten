class Node {

	constructor(x,y,z,lastBiome,lastMotiff,lastLocation,directionBack) {
		this.x = x,
		this.y = y,
		this.z = z,
		this.lastBiome = lastBiome,
		this.lastMotiff = lastMotiff,
		this.lastLocation = lastLocation,
		this.directionBack = directionBack
	}
}

export default Node